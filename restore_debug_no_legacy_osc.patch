diff --git a/app/src/main/java/com/example/tapsyncwatch/presentation/MainActivity.kt b/app/src/main/java/com/example/tapsyncwatch/presentation/MainActivity.kt
index 1111111..2222222 100644
--- a/app/src/main/java/com/example/tapsyncwatch/presentation/MainActivity.kt
+++ b/app/src/main/java/com/example/tapsyncwatch/presentation/MainActivity.kt
@@
-    // Debounce hardware back/stem so it doesn't skip pages (Debug → FFT → Tap)
+    // Debounce hardware back/stem so it doesn't skip pages (Debug → FFT → Tap)
     private var lastBackKeyMs: Long = 0L
@@
     override fun onKeyDown(keyCode: Int, event: KeyEvent?): Boolean {
         return when (keyCode) {
             KeyEvent.KEYCODE_BACK,
+            KeyEvent.KEYCODE_STEM_1,
             KEYCODE_STEM_2 -> {
                 // Force the bottom hardware key to behave as Back everywhere,
                 // but debounce it so we don't skip Debug → FFTGain → Tap.
                 val now = SystemClock.elapsedRealtime()
-                if (now - lastBackKeyMs < 240L) return true
+                if (now - lastBackKeyMs < 480L) return true
                 lastBackKeyMs = now
                 onBackPressedDispatcher.onBackPressed()
                 true
             }
+            KeyEvent.KEYCODE_STEM_3 -> {
+                val now = SystemClock.elapsedRealtime()
+                if (now - lastBackKeyMs < 480L) return true
+                lastBackKeyMs = now
+                onBackPressedDispatcher.onBackPressed()
+                true
+            }
 
             else -> super.onKeyDown(keyCode, event)
         }
     }
@@
                         HomePage.TAP -> TapScreen(
@@
-                            showOscDebug = s.showOscDebug,
+                            // Legacy fullscreen OSC monitor disabled (it blocks gestures & regresses Debug access).
+                            // New OSC Monitor lives in DebugScreen only.
+                            showOscDebug = false,
                             oscDebugState = oscReceiver.debugState,
                             remoteGhost = oscReceiver.remoteGhost,
                             phaseVisualizerEnabled = s.phaseVisualizerEnabled,
                             phaseSpiralEnabled = s.phaseSpiralEnabled,
                              fxAlpha = s.fxAlpha,
diff --git a/app/src/main/java/com/example/tapsyncwatch/presentation/ui/TapScreen.kt b/app/src/main/java/com/example/tapsyncwatch/presentation/ui/TapScreen.kt
index 3333333..4444444 100644
--- a/app/src/main/java/com/example/tapsyncwatch/presentation/ui/TapScreen.kt
+++ b/app/src/main/java/com/example/tapsyncwatch/presentation/ui/TapScreen.kt
@@
-        // OSC Monitor: fullscreen, scrollable, close (X) bottom-center
-        if (showOscDebug) {
+        // OSC Monitor: legacy fullscreen overlay (DISABLED).
+        // This overlay blocks swipe/tap gestures and makes Debug feel "gone".
+        if (false && showOscDebug) {
             val dbg by oscDebugState.collectAsState(initial = OscInputReceiver.OscDebugState())
             val scroll = rememberScrollState()
             val isRound = LocalConfiguration.current.isScreenRound
             val edgePad = if (isRound) 18.dp else 12.dp
             val topPad = if (isRound) 14.dp else 12.dp
             val bottomPad = if (isRound) 16.dp else 12.dp